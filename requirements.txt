# Python dependencies for the GoEmotions inference layer
# (emotion_mapping.py, emotion_api.py, emotion_validation.py).
#
# Install into the project venv from CustomerAgent/:
#   ..\.venv\Scripts\python.exe -m pip install -r requirements.txt
transformers==5.17.0
torch==2.14.0
Flask==3.1.3
flask-cors==6.0.5
